
const Button = ({title}) => {
    return (
        <div>
            <button className="text-white btn btn-outline border-0 border-b-4">{title}</button>
        </div>
    );
};

export default Button;